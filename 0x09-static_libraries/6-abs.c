#include "main.h"
#include <stdlib.h>

/**
 * _abs - write a func that computes the absolute value of
 * an int
 * @n: one integer
 *
 * Return: num
 */

int _abs(int n)
{
	int num;

	num = abs(n);

	return (num);
}
